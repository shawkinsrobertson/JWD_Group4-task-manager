class taskManager {
   constructor(task, currentId) {
       this.task = [];
       this.currentId = 0;

       addTask(name, description, assignedTo, dueDate, status) {
           this.currentId ++;
           this.task.push()
       }
   }
}